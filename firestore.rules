rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all users (or authenticated users)
    match /{document=**} {
      allow read, write: if request.auth != null || true;
    }
  }
}
